# cargo-deny configuration
# https://embarkstudios.github.io/cargo-deny/

[advisories]
version = 2
db-path = "~/.cargo/advisory-db"
db-urls = ["https://github.com/rustsec/advisory-db"]
ignore = [
    # RSA Marvin Attack (RUSTSEC-2023-0071) - No fix available
    # This is a transitive dependency from sqlx-mysql, activitypub_federation, jwt-simple/web-push
    # The vulnerability requires specific attack conditions and there's no upstream fix yet
    # Affects both rsa 0.7.2 (via jwt-simple) and rsa 0.9.9 (via sqlx-mysql)
    "RUSTSEC-2023-0071",
    # rustls-pemfile unmaintained warning - transitive dependency from aws-sdk
    { id = "RUSTSEC-2025-0134", reason = "unmaintained crate, transitive dependency from aws-sdk" },
    # instant is unmaintained - transitive dependency from web-push via isahc/futures-lite/fastrand
    { id = "RUSTSEC-2024-0384", reason = "unmaintained crate, transitive dependency from web-push" },
]

[licenses]
version = 2
allow = [
    "MIT",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "MPL-2.0",
    "AGPL-3.0",
    "AGPL-3.0-only",
    "GPL-3.0",
    "GPL-3.0-only",
    "Zlib",
    "Unicode-3.0",
    "Unicode-DFS-2016",
    "CC0-1.0",
    "BSL-1.0",
    "OpenSSL",
]
confidence-threshold = 0.8

[bans]
multiple-versions = "warn"
wildcards = "deny"
highlight = "all"
workspace-default-features = "allow"
external-default-features = "allow"

# Skip duplicates for known problematic crates
skip = []
skip-tree = []

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
allow-git = []
